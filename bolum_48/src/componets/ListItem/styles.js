import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  item_container : { margin: 10, padding: 8, backgroundColor: "#ccc", borderRadius: 10,flex : 1},
  item_text : {color : "black"}
});

export default styles;
